---
description: "Manage MCP servers with profiles and toggles"
---

I need to manage MCP servers. Please help me with the following options:

**Available Commands:**

1. **List all servers** - Show all 22 MCP servers organized by category with their current status
2. **Enable servers** - Turn on specific MCP servers (provide server names)
3. **Disable servers** - Turn off specific MCP servers (provide server names)
4. **Switch profile** - Change to a predefined profile:
   - `minimal` - 3 essential servers (memory, filesystem, sequential-thinking)
   - `dev` - 7 development servers (adds github, netlify, vercel, digitalocean)
   - `full` - All 22 servers enabled
5. **Show status** - Display current configuration and detect active profile

**MCP Servers Available (22 total):**

**Core Services:**
- memory, filesystem, sequential-thinking

**Development Tools:**
- github, netlify, vercel, digitalocean, railway, figma-developer, cursor-talk-to-figma

**Web & Content:**
- firecrawl, fetch, puppeteer, dataforseo, nano-banana

**Integrations:**
- cloudinary, replicate, airtable, n8n-mcp, gohighlevel, apify-modern, MCP_DOCKER

**Usage Examples:**
- "List all MCP servers"
- "Switch to minimal profile"
- "Enable github and netlify"
- "Disable puppeteer and dataforseo"
- "Show current MCP status"

Please run the appropriate npm script from package.json to execute my request.
